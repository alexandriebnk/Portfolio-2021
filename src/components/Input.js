import React from "react";

const Input = () => {
  return (
    <div className={"input"}>
      <input
        className={"input_element"}
        /*type={type}
        placeholder={placeholder}
        onChange={onChange}*/
      />
    </div>
  );
};

export default Input;
